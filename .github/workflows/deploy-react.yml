# Name of the workflow
name: Deploy React App to GitHub Pages

on:
  # Runs on pushes targeting the main branch
  push:
    branches: ["main"]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # This step sets up caching for npm dependencies
          cache: 'npm'

      - name: Force Clean Install
        run: |
          # The following lines are NOT recommended for production workflows.
          # They force a slow and potentially unstable dependency resolution.
          rimraf ./node_modules
          rimraf ./package-lock.json
          npm install

      - name: Build the React app
<<<<<<< .mine
        # *** THIS IS THE FIX ***
        # The build job should ONLY build the project, not deploy it.
        run: npm run build 
        env:
          # This is needed if you have warnings that you want to treat as errors
          CI: false
=======
        run: npm run deploy # This creates the 'build' folder with your static site





>>>>>>> .theirs

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload the 'build' folder created by 'npm run build'
          path: './build'

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    # This job depends on the 'build' job finishing successfully
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4